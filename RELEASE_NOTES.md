# v0.1.0 开源整理说明

本版将私人生产环境适配包整理为可公开分发的电商 Skill。保留上游 MIT 许可，使用独立名称 `h3-ecommerce-director`。原始导出包未修改，本目录不包含其私人运行记录。

## 已完成

- 删除私人主机、用户名、绝对业务目录、人员姓名、表格/任务标识、工作台地址、历史批次与可继承的客户授权。
- 保留并去客户化整理商品一致性、角色优化、参考还原、表演、连续性、画质与返修规范。
- 重写安装和能力说明；补公开配置、合成陶瓷杯样例、Python依赖、离线测试和CI。
- 修复首尾条件混排和静默丢弃、POST超时盲重试、下载失败重新生成、重试次数计数、dry-run上传及审核拒绝自动备用。
- 增加项目锁、提交快照、未知任务核对、多输出保留及参考内容变更检查；已提交版本不可原地重建。
- 就绪检查不依赖历史批次；补媒体完整解码检查与取帧覆盖保护。
- 飞书、fast专用云视频、双ASR、工作台、剪映和后台同步明确标为未随包部署的可选能力。

## 已执行验证

- 34项离线单元/回归/包结构测试通过。
- Skill格式校验通过，Python/JSON解析及本地文档链接检查通过。
- 使用本地模拟HTTP服务完成10项命令级检查：构建、dry-run、就绪探测、首次提交、重复提交跳过、监控下载、媒体解码、缺音轨阻断、指定区间抽帧、未完成审片阻断。
- 模拟流程只产生一次生成POST；下载结果保留needs_review。
- 基础凭据模式、业务标识、私网IP与个人home路径扫描未发现匹配。本次检查不能替代使用者对后续加入文件的复核。
- 原始53个文件与原导出SHA256清单仍一致。

未执行：真实H3 GPU成片、付费图片API兼容性、第三方云视频与工作台集成、Windows运行验证。CI工作流已提供，远端Actions尚未运行。实际发布后应在对应环境记录节点/模型版本和代表镜头效果，不能以离线测试宣称视频质量已验收。

## 使用与发布

本目录可作为独立仓库内容，许可证及上游来源文件应一并保留。公开仓库为 https://github.com/xydzf520/h3-ecommerce-director ，README 已加入源码获取命令；保留上游版权声明并标注本版修改者。

`FILE_SHA256.json` 校验本目录其他发行文件，不包含自身。发布前或下载后可按其中的相对文件名计算SHA256核对；不要将实际生产工作区、凭据、客户媒体和测试产生的私有路径加入仓库。
